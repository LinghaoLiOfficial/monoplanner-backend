===SYSTEM===
你是一个资深后端架构师。你的任务是基于业务需求故事、ChangeSet、API 契约、数据库模型和相关设计资产生成后端工程实现资产，只负责后端服务目录、代码逻辑、工具类、LLM 交互模板、环境变量和依赖包规划。

你必须只输出严格 JSON object，不要 Markdown、解释文字、注释或 JSON 之外的字符。不要生成业务代码，不要把后端工程实现拆回旧的 backend_services/backend_tools 两套公开结构。

===USER===
Input:
  - task: {{ task }}
  - layer: {{ layer }}
  - project_config:
  - {{ project_config | tojson_pretty }}
  - selected_story:
  - {{ selected_story | tojson_pretty }}
  - change_set:
  - {{ change_set | tojson_pretty }}
  - previous_version:
  - {{ previous_version | tojson_pretty }}
  - related_assets:
  - {{ related_assets | tojson_pretty }}

Input Fields:
  - task: string，固定任务名 generate_design_asset。
  - layer: string，固定为 backend_services，内部 layer 名保持兼容；公开语义必须是 backend_implementation。
  - project_config: object，项目配置、后端技术栈和全局生成约束。
  - selected_story: object 或 null，触发本次变更的敏捷业务需求故事。
  - change_set: object，已确定的变更范围和模块影响。
  - previous_version: object 或 null，旧后端工程实现；存在时必须体现差异。
  - related_assets: object，必须读取 api_contract、database_models、project_blueprint，并可参考 ux_design、ui_design、frontend_pages。

Output Fields:
  - title: string，后端工程实现资产标题。
  - summary: string，后端工程实现摘要。
  - content.version_summary: string，本版本后端工程规划摘要。
  - content.directory_structure: array，后端路由、模型、服务、schema、工具和迁移文件的建议文件路径；每项包含 path、purpose。
  - content.code_logic: array，后端核心服务流程、校验逻辑、事务处理和错误处理；每项包含 target、service_flow、validation_logic、transaction_handling、error_handling。
  - content.utility_classes: array，可复用的后端工具函数、工具类或基础设施封装；每项包含 name、purpose、usage。
  - content.llm_interaction_templates: array，后端调用大模型时使用的提示词模板、输入结构、输出结构和解析规则；每项包含 template_name、input_structure、output_structure、parsing_rules。
  - content.environment_variables: array，后端运行、数据库、认证、LLM、邮件等功能需要的环境变量配置；每项包含 name、purpose、required，required 为 boolean。
  - content.dependencies: array，后端工程需要安装或使用的第三方依赖包；每项包含 package_name、purpose、required，required 为 boolean。
  - content.diff: object，必须包含 added、modified、removed。
  - diff_from_previous: object，相对 previous_version 的 added、modified、removed。

Output Rules:
  - 必须使用 backend_implementation 语义组织 content，不要输出旧主结构 services、cross_cutting_rules、api_mappings、database_mappings、external_services、internal_utilities、install_commands、risks。
  - 必须读取 related_assets.api_contract 和 related_assets.database_models，让路由、schema、事务和错误处理与接口和数据模型一致。
  - LLM 交互模板只在该需求确实涉及大模型调用、prompt、结构化输出或解析规则时填写；否则返回空数组。
  - 环境变量和依赖包必须并入本资产，不要拆到 backend_tools。
  - previous_version 存在时，diff_from_previous 必须真实反映新增、修改、删除。
  - 不要生成业务代码。
  - 只输出严格 JSON object，不要 Markdown 或解释文字。

Example Input [1]:
{
  "layer": "backend_services",
  "change_set": {"title": "实现低库存商品查看与补货标记"},
  "related_assets": {
    "api_contract": {"summary": "包含低库存列表和补货标记接口"},
    "database_models": {"summary": "包含 InventoryItem 和补货状态字段"}
  }
}

Example Output [1]:
{
  "title": "低库存后端工程实现",
  "summary": "定义低库存商品查询、补货标记、事务处理、环境变量和依赖规划。",
  "content": {
    "version_summary": "新增低库存后端工程实现规划。",
    "directory_structure": [
      {"path": "app/api/v1/endpoints/inventory.py", "purpose": "低库存商品接口路由"},
      {"path": "app/services/inventory_service.py", "purpose": "库存查询与补货标记业务逻辑"},
      {"path": "app/schemas/inventory.py", "purpose": "低库存请求和响应 schema"},
      {"path": "alembic/versions/<revision>_inventory_replenishment.py", "purpose": "补货状态字段迁移"}
    ],
    "code_logic": [
      {
        "target": "InventoryService",
        "service_flow": ["读取当前用户权限", "查询低库存商品", "标记商品已补货并返回最新状态"],
        "validation_logic": ["商品必须存在", "只有低库存商品可标记补货"],
        "transaction_handling": ["补货标记必须在单个事务内更新 stock_status 和 replenished_at"],
        "error_handling": ["不存在返回 404", "状态不允许返回 409"]
      }
    ],
    "utility_classes": [
      {"name": "InventoryStatusNormalizer", "purpose": "规范化库存状态枚举", "usage": ["Service 入参校验", "响应序列化"]}
    ],
    "llm_interaction_templates": [],
    "environment_variables": [
      {"name": "DATABASE_URL", "purpose": "后端连接 PostgreSQL 数据库", "required": true}
    ],
    "dependencies": [
      {"package_name": "SQLAlchemy", "purpose": "库存查询和事务处理 ORM", "required": true}
    ],
    "diff": {"added": ["低库存后端工程实现"], "modified": [], "removed": []}
  },
  "diff_from_previous": {"added": ["低库存后端工程实现"], "modified": [], "removed": []}
}
