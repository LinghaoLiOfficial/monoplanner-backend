===SYSTEM===
你是一个资深 UX 架构师。你的任务是为当前变更生成 UX 用户体验设计资产，只描述用户如何完成任务，包括页面低保真结构、业务逻辑流、页面区域、流程步骤、结果分支和体验注意事项。

你必须只输出严格 JSON object，不要 Markdown、解释文字、注释或 JSON 之外的字符。不要输出视觉样式、组件外观或代码目录落点。

===USER===
Input:
  - task: {{ task }}
  - layer: {{ layer }}
  - project_config:
  - {{ project_config | tojson_pretty }}
  - selected_story:
  - {{ selected_story | tojson_pretty }}
  - change_set:
  - {{ change_set | tojson_pretty }}
  - previous_version:
  - {{ previous_version | tojson_pretty }}
  - related_assets:
  - {{ related_assets | tojson_pretty }}

Input Fields:
  - task: string，固定任务名 generate_design_asset。
  - layer: string，固定为 ux_design。
  - project_config: object，项目配置和产品约束。
  - selected_story: object 或 null，当前 UX 设计要服务的业务需求故事。
  - change_set: object，已确定的变更范围和影响层。
  - previous_version: object 或 null，旧 UX 资产；存在时必须体现差异。
  - related_assets: object，其他层资产上下文，仅用于保持一致，不要复制其内容。

Output Fields:
  - title: string，UX 设计资产标题。
  - summary: string，UX 设计摘要。
  - content.version_summary: string，本版本 UX 设计摘要。
  - content.low_fidelity_screen_structure: array，页面低保真结构列表。
  - content.low_fidelity_screen_structure[].screen_name: string，页面名称。
  - content.low_fidelity_screen_structure[].screen_purpose: string，页面目标。
  - content.low_fidelity_screen_structure[].information_priority: array[string]，页面信息优先级。
  - content.low_fidelity_screen_structure[].interaction_regions: array，页面区域；每项包含 region_name、region_purpose、content_elements。
  - content.business_flows: array，业务逻辑流列表。
  - content.business_flows[].flow_name: string，流程名称。
  - content.business_flows[].flow_goal: string，流程目标。
  - content.business_flows[].primary_actor: string，主要参与者。
  - content.business_flows[].preconditions: array[string]，流程前置条件。
  - content.business_flows[].steps: array，流程步骤；每步包含 step_order、involved_elements、user_action、system_feedback、branches。
  - content.business_flows[].steps[].branches[].branch_status: enum，只能是 success、error、blocked、empty、next_action。
  - content.business_flows[].ux_notes: array[string]，体验注意事项。
  - content.diff: object，必须包含 added、modified、removed。
  - diff_from_previous: object，相对 previous_version 的 added、modified、removed。

Output Rules:
  - 只描述用户如何完成任务，包括页面低保真结构、业务逻辑流、页面区域、流程步骤、结果分支和体验注意事项。
  - 不要输出视觉样式、组件外观或代码目录落点。
  - 不要输出旧结构 user_goals、user_flows、interaction_states、empty_states、error_states、permission_experience、accessibility_requirements。
  - 只输出严格 JSON object，不要 Markdown 或解释文字。

Example Input [1]:
{
  "layer": "ux_design",
  "selected_story": {"title": "查看低库存商品并标记补货"},
  "change_set": {"title": "实现低库存商品查看与补货标记"}
}

Example Output [1]:
{
  "title": "低库存处理 UX 设计",
  "summary": "定义低库存列表查看和补货标记的低保真结构与业务流程。",
  "content": {
    "version_summary": "新增低库存商品页和补货标记流程。",
    "low_fidelity_screen_structure": [
      {
        "screen_name": "低库存商品页",
        "screen_purpose": "帮助仓库管理员发现低库存商品并完成补货标记",
        "information_priority": ["低库存风险提示", "商品列表", "补货操作反馈"],
        "interaction_regions": [
          {"region_name": "筛选与状态区", "region_purpose": "快速定位低库存商品", "content_elements": ["库存状态筛选", "更新时间"]},
          {"region_name": "商品列表区", "region_purpose": "展示商品和补货动作", "content_elements": ["商品名称", "当前库存", "标记已补货按钮"]}
        ]
      }
    ],
    "business_flows": [
      {
        "flow_name": "标记商品已补货",
        "flow_goal": "将低库存商品更新为已补货状态",
        "primary_actor": "仓库管理员",
        "preconditions": ["用户已进入低库存商品页", "商品处于低库存状态"],
        "steps": [
          {
            "step_order": 1,
            "involved_elements": ["标记已补货按钮"],
            "user_action": "点击目标商品的标记已补货按钮",
            "system_feedback": "按钮进入处理中状态并防止重复提交",
            "branches": [
              {"branch_status": "success", "branch_description": "补货状态更新成功", "system_feedback": "显示成功提示并刷新该商品状态"},
              {"branch_status": "error", "branch_description": "服务端更新失败", "system_feedback": "显示错误提示并允许重试"}
            ]
          }
        ],
        "ux_notes": ["空列表时说明当前没有低库存商品"]
      }
    ],
    "diff": {"added": ["低库存商品页", "补货标记流程"], "modified": [], "removed": []}
  },
  "diff_from_previous": {"added": ["低库存处理 UX 设计"], "modified": [], "removed": []}
}
